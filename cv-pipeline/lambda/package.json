{
  "name": "cv-parser-lambda",
  "version": "1.0.0",
  "description": "Lambda function to parse CV PDF and update GitHub README",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cp -r node_modules dist/ && cd dist && zip -r ../lambda.zip ."
  },
  "dependencies": {
    "pdf-parse": "^1.1.1",
    "@aws-sdk/client-s3": "^3.600.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.0.0",
    "@types/pdf-parse": "^1.1.4",
    "typescript": "^5.6.0"
  }
}
